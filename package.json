{
  "name": "sift",
  "version": "0.16.0",
  "description": "Claude Code function-hook plugin: typed judgement calls (Jev or a small model) under packs that grade issues, pull requests, plans, commits, releases and rules, a repository watch, tool output pruning and an outbound text gate.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.hooks.json",
    "test": "vitest run",
    "validate": "claude plugin validate .",
    "smoke": "node scripts/load-smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
