{
  "name": "sift",
  "version": "0.5.1",
  "description": "Claude Code function-hook plugin: typed judgement calls (Jev or a small model) for compaction, tool output pruning, repo watching and grading.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.hooks.json",
    "test": "vitest run",
    "validate": "claude plugin validate ."
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
